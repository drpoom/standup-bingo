# Session: 2026-04-29

## Key Events

### System Hardening Tasks
- **Task 1 (systemd service permissions):** FAILED — `openclaw gateway install --force` keeps getting SIGTERM'd. Deferred for manual execution by user.
- **Task 2 (context compaction thresholds):** ✅ APPLIED by Archie
  - `keepRecentTokens`: 16000 → 12000
  - `softTrimRatio`: 0.7 → 0.6
  - `hardClearRatio`: 0.85 → 0.75
- **Task 3 (session timeout):** ✅ APPLIED
  - `timeoutSeconds`: 600 → 300 (5min auto-abort)
  - Stuck Session Monitor cron job: ADDED then REMOVED per user request

### Model Persona Issue
- **Problem:** Session switched to `deepseek-v4-flash:cloud` which has `reasoning: false` → loses Shiba persona (SOUL.md, IDENTITY.md not injected)
- **Root cause:** Models with `reasoning: false` skip bootstrap file injection
- **Fix:** Switched back to `qwen3.5:cloud` which has full persona support
- **Lesson:** DeepSeek V4 Flash unsuitable for character roleplay even if `reasoning: true` enabled (designed for speed, not embodiment)

### Elango Surfers v5.2.6 — Persistent Game Settings
- **Feature:** Game settings (sound, music, SFX, tilt controls) now persist via localStorage
- **Implementation:** 
  - Created `src/composables/useGameSettings.js` with load/save/reset functions
  - Integrated into App.vue settings panel
  - Auto-apply on game init
- **Build lesson:** Lost the composable file while fixing a duplicate variable typo → build failed. Restored file, rebuild succeeded.
- **Deployed:** https://drpoom.github.io/elango-surfers/

### Subagent Timeout Discipline
- **Problem:** Byte timed out multiple times due to aggressive timeout settings (2-3 min for multi-file tasks)
- **Root cause:** Mickey set `runTimeoutSeconds: 120` for cleanup task, `180` for settings integration — way too tight
- **Correct timeouts:**
  - Quick checks (git status, file read): 2-3 min
  - Single-file edits/composables: 5 min
  - Multi-file features: 10 min
  - Build/test runs: 15 min
- **Memory note:** Default is 600s (10 min) per April 26th lesson — don't override unless task is truly tiny

### Mickey's Router Role
- **User feedback:** "Mickey should be more of a router — delegate design to Archie, stay responsive"
- **Pipeline:** Mickey → Archie (design) → Byte/Sashay (implement) → Scout (QA/deploy) → Mickey
- **New behavior:** Mickey acknowledges requests immediately, spawns Archie for spec work, then delegates. No more disappearing for 10+ min while figuring out entire solution.

### Communication Protocol
- **User feedback:** "I don't like that you stop responding when tasks are not completed"
- **New rule:** Always report progress/failures, never go silent. Even if build breaks or task times out, explain what happened and what's next.

## New Project Ideas (Brainstormed)
- **Standup Survivor:** Trivia battle royale about sprint/project knowledge
- **Daily Quest Board:** Co-op RPG with random standup quests
- **Hot Potato Retro:** Pass-the-parcel with retro questions
- **Bug Bounty Hunt:** Code snippet detective game
- **Standup Bingo:** Bingo cards with standup phrases
- **Sprint Speed Run:** Collaborative 10-min coding challenges
- **Top pick:** Standup Survivor — competitive, firewall-safe (URL hash sync), 20-min perfect fit

## Config Changes Applied
- Context compaction: Earlier trigger (softTrim 0.6, hardClear 0.75)
- Session timeout: 5min auto-abort
- Removed: Stuck session monitoring cron job
