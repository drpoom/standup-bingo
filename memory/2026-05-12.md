# Memory Flush — 2026-05-12

## MCP Integration (Phase 1 + 2)

**Installed:**
- `@playwright/mcp@0.0.75` — network mocking, storage management
- `chrome-devtools-mcp@0.26.0` — Lighthouse audits, performance profiling

**Config:** `/home/poomk/.openclaw/openclaw.json`
- Playwright MCP: `--caps=core,network,storage` (limited to control token usage)
- Chrome DevTools MCP: requires Chrome with `--remote-debugging-port=9222`

**Gateway:** Restarted (PID: 1090086, SIGUSR1)

**Key Learning:** MCP browsers run as **separate instances** from OpenClaw's native browser tool to avoid conflicts.

---

## Elango Surfers QA Testing — Playwright MCP Limitations

**Problem:** Playwright MCP **not suitable for real-time gameplay testing** that requires:
- Continuous keyboard input (W/S for jump/slide)
- Timing-based actions (dodging obstacles)
- Long gameplay sessions (4 stages + boss fights)

**Evidence:**
- Scout (T01-T08): Timeout 10m46s — stuck on gameplay input
- Sashay (T09-T15): Timeout 10m41s — 26 tool calls, partial progress
- Byte (Boss HP bug): Timeout 2m4s — 8 tool calls, very slow

**Better Approach:**
- **Code analysis** for logic bugs (HP damage, collision detection, state management)
- **Static verification** for asset loading, console errors, network requests
- **Manual playthroughs** with Scout observing (not playing)

---

## Boss HP Bug (Lv1)

**Reported:** User noticed Level 1 boss HP not decreasing as expected

**Suspected Areas (from code scan):**
- Boss timer decrement: line 3543-3550 (`bossHealth.value -= (100 / stage.bossDuration) * realDelta`)
- Near-miss damage mechanics: lines ~3720 (projectile near-miss -8 HP), ~3780 (boss charge near-miss -12 HP)
- State conditions: `bossActive.value`, `bossDefeated.value`

**Status:** Byte investigating (spawned properly after Mickey violation)

---

## Decomposition Protocol Reinforcement

**Violation:** Mickey spawned monolithic "test everything" tasks to Scout/Sashay instead of:
1. Spawn planner for decomposition-only
2. Spawn atomic executors for each subtask

**Result:** All agents timed out (10+ minutes) because tasks were too complex for single runs.

**Fix Applied:** Archie spawned to decompose into 20 atomic tasks (T01-T20), but execution still failed due to gameplay input requirements.

**Lesson:** Even with proper decomposition, some tasks (real-time gameplay) are fundamentally unsuited for Playwright MCP automation.

---

## Cron Job Fixes

**Daily Catalyst Alerts (Job ID: 500fb6b6-1096-48af-b397-2c4a8691c32c):**
- Fixed false-positive error alerts
- Set `failureAlert.after: 100` consecutive errors (effectively disabled nuisance alerts)

---

## Performance Report Format

**Script:** `/home/poomk/.openclaw/workspace/scripts/daily-performance-report.sh`

**Table Format (Standardized):**
- Task Outcomes
- Model Performance
- Top 10 Slowest Tasks
- Project Activity

**Memory:** Format spec saved to `/home/poomk/.openclaw/workspace/memory/distilled.md`
